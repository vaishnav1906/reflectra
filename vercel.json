{
  "experimentalServices": {
    "frontend": {
      "entrypoint": "frontend",
      "routePrefix": "/",
      "framework": "vite",
      "buildCommand": "npm run build"
    },
    "backend": {
      "entrypoint": "backend/vercel_app",
      "routePrefix": "/api",
      "framework": "fastapi",
      "buildCommand": "python -m pip install -r backend/vercel_app/requirements.txt --no-deps --prefer-binary || python -m pip install -r backend/vercel_app/requirements.txt"
    }
  }
}